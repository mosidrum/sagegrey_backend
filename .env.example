PORT=8000

DB_HOST=postgres
DB_PORT=5432
DB_NAME=app
DB_USER=postgres
DB_PASSWORD=postgres
NODE_ENV=development

# Comma-separated list of allowed origins for CORS. Leave unset to allow any
# origin (e.g. in local development).
CORS_ORIGIN=
